# Get a free API key at https://fred.stlouisfed.org/useraccount/apikey
# Used to fetch the current Freddie Mac 30-yr fixed mortgage rate (MORTGAGE30US series).
# Without this, the APR slider uses a static default range.
VITE_FRED_API_KEY=
